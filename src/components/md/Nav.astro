---
import { SITE } from '../../config/site';
const navLinks = [
  { href: '/', label: 'Startseite' },
  { href: '/webdesign', label: 'Webdesign' },
  { href: '/#referenzen', label: 'Referenzen' },
  { href: '/ueber-uns', label: 'Über uns' },
  { href: '/blog', label: 'Blog' },
  { href: '/vertrieb', label: 'Vertriebspartner' },
  { href: '/#kontakt', label: 'Kontakt' },
];
const currentPath = Astro.url.pathname;
---

<header class="md-nav-wrap" role="banner">
  <nav class="md-nav md-container" aria-label="Hauptnavigation">
    <!-- Logo -->
    <a href="/" class="md-nav-logo" aria-label="MediaDrift – Zur Startseite">
      <img src="/images/logo.jpeg" alt="MediaDrift Logo" class="logo-img" width="34" height="34" />
      <span class="logo-text">MediaDrift</span>
    </a>

    <!-- Desktop Links -->
    <ul class="md-nav-links" role="list">
      {navLinks.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class={`md-nav-link ${(href === '/' ? currentPath === '/' : currentPath.startsWith(href)) ? 'active' : ''}`}
            aria-current={(href === '/' ? currentPath === '/' : currentPath.startsWith(href)) ? 'page' : undefined}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- CTA -->
    <a href="/#kontakt" class="md-btn md-btn-primary md-nav-cta">
      Entwurf anfragen
    </a>

    <!-- Hamburger (mobile) -->
    <button
      class="md-nav-burger"
      aria-label="Menü öffnen"
      aria-expanded="false"
      aria-controls="mobile-menu"
      id="burger-btn"
    >
      <span></span><span></span><span></span>
    </button>
  </nav>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="md-mobile-menu" aria-hidden="true" role="dialog" aria-label="Mobilmenü">
    <ul role="list">
      {navLinks.map(({ href, label }) => (
        <li>
          <a href={href} class="md-mobile-link">{label}</a>
        </li>
      ))}
    </ul>
    <a href="/#kontakt" class="md-btn md-btn-primary" style="margin-top: 1.5rem; width: 100%; justify-content: center;">
      Entwurf anfragen
    </a>
  </div>
</header>

<!-- Skip link -->
<a href="#main-content" class="md-skip-link">Zum Inhalt springen</a>

<style>
  .md-skip-link {
    position: fixed;
    top: -100px;
    left: 1rem;
    background: var(--md-cta);
    color: #fff;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-family: 'Jost', system-ui, sans-serif;
    font-size: 0.9rem;
    z-index: 9999;
    transition: top 150ms ease;
  }
  .md-skip-link:focus { top: 1rem; }

  .md-nav-wrap {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(10, 10, 10, 0.96);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    height: var(--nav-h);
    transition: box-shadow 200ms ease;
  }
  .md-nav-wrap.scrolled {
    box-shadow: 0 2px 32px rgba(0, 0, 0, 0.55);
  }

  .md-nav {
    display: flex;
    align-items: center;
    gap: 2rem;
    height: var(--nav-h);
  }

  .md-nav-logo {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    flex-shrink: 0;
  }
  .logo-img {
    width: 34px;
    height: 34px;
    border-radius: 8px;
    object-fit: cover;
    transition: transform 200ms var(--ease-out);
  }
  @media (hover: hover) and (pointer: fine) {
    .md-nav-logo:hover .logo-img { transform: scale(1.06) rotate(-3deg); }
  }
  .md-nav-logo:active .logo-img { transform: scale(0.96); transition-duration: 80ms; }
  .logo-mark {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    background: var(--md-cta);
    color: #fff;
    border-radius: 8px;
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: 1.1rem;
  }
  .logo-text {
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: 1.15rem;
    color: #FAFAFA;
    font-weight: 400;
  }

  .md-nav-links {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    list-style: none;
    margin: 0;
    padding: 0;
    flex: 1;
  }

  .md-nav-link {
    position: relative;
    padding: 0.4rem 0.7rem;
    border-radius: 5px;
    font-size: 0.9rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.68);
    text-decoration: none;
    transition: color 150ms var(--ease-out), background-color 150ms var(--ease-out);
  }
  /* Animated underline that grows from center */
  .md-nav-link::after {
    content: '';
    position: absolute;
    left: 50%;
    right: 50%;
    bottom: 2px;
    height: 2px;
    border-radius: 2px;
    background: var(--md-cta-dark, #0284C7);
    transition: left 200ms var(--ease-out), right 200ms var(--ease-out);
  }
  @media (hover: hover) and (pointer: fine) {
    .md-nav-link:hover { color: #fff; background: rgba(255, 255, 255, 0.08); }
    .md-nav-link:hover::after { left: 0.7rem; right: 0.7rem; }
  }
  .md-nav-link.active { color: #fff; font-weight: 500; }
  .md-nav-link.active::after { left: 0.7rem; right: 0.7rem; }

  .md-nav-cta {
    flex-shrink: 0;
    font-size: 0.9rem;
    font-weight: 600;
    padding: 0.62rem 1.35rem;
    border-radius: 999px;
    letter-spacing: 0.01em;
    background: linear-gradient(135deg, #0EA5E9 0%, #0369A1 100%);
    box-shadow: 0 4px 14px rgba(14, 165, 233, 0.35);
    transition:
      background 200ms var(--ease-out),
      box-shadow 200ms var(--ease-out),
      transform 200ms var(--ease-out);
  }
  @media (hover: hover) and (pointer: fine) {
    .md-nav-cta:hover {
      background: linear-gradient(135deg, #38BDF8 0%, #0284C7 100%);
      box-shadow: 0 6px 20px rgba(14, 165, 233, 0.5);
      transform: translateY(-1px);
    }
  }

  /* Hamburger */
  .md-nav-burger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    margin-left: auto;
  }
  .md-nav-burger span {
    display: block;
    width: 22px;
    height: 2px;
    background: #fff;
    border-radius: 2px;
    transition: transform 200ms ease, opacity 200ms ease;
  }
  .md-nav-burger[aria-expanded="true"] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .md-nav-burger[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .md-nav-burger[aria-expanded="true"] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  /* Mobile menu */
  .md-mobile-menu {
    display: none;
    position: fixed;
    top: var(--nav-h);
    left: 0;
    right: 0;
    background: #0A0A0A;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    padding: 1.5rem clamp(1rem, 5vw, 3rem);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.6);
  }
  .md-mobile-menu.open { display: block; }
  .md-mobile-menu ul { list-style: none; margin: 0; padding: 0; }
  .md-mobile-link {
    display: block;
    padding: 0.9rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    color: rgba(255, 255, 255, 0.8);
    font-size: 1rem;
    font-weight: 400;
    text-decoration: none;
    font-family: 'Jost', system-ui, sans-serif;
  }
  .md-mobile-link:hover { color: #fff; }

  @media (max-width: 900px) {
    .md-nav-links, .md-nav-cta { display: none; }
    .md-nav-burger { display: flex; }
  }
</style>

<script>
  const burger = document.getElementById('burger-btn') as HTMLButtonElement;
  const menu = document.getElementById('mobile-menu') as HTMLElement;
  const header = document.querySelector('.md-nav-wrap') as HTMLElement;

  burger?.addEventListener('click', () => {
    const expanded = burger.getAttribute('aria-expanded') === 'true';
    burger.setAttribute('aria-expanded', String(!expanded));
    menu.setAttribute('aria-hidden', String(expanded));
    menu.classList.toggle('open');
    burger.setAttribute('aria-label', expanded ? 'Menü öffnen' : 'Menü schließen');
  });

  // Close on ESC
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && menu.classList.contains('open')) {
      burger.setAttribute('aria-expanded', 'false');
      menu.setAttribute('aria-hidden', 'true');
      menu.classList.remove('open');
      burger.focus();
    }
  });

  // Scroll shadow
  const onScroll = () => header?.classList.toggle('scrolled', window.scrollY > 10);
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();

  // Close mobile menu on link click
  menu?.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => {
      burger.setAttribute('aria-expanded', 'false');
      menu.setAttribute('aria-hidden', 'true');
      menu.classList.remove('open');
    });
  });
</script>
